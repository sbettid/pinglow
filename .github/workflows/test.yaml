name: Pinglow CI

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  rust-ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Run tests
        run: cargo test --all

  doc-ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Generate openapi specification
        run: cargo run --bin generate-openapi

      - name: Install dependencies
        working-directory: docs
        run: npm ci

      - name: Generate dyanmic openapi menu
        working-directory: docs
        run: npx ts-node-esm src/scripts/generateOpenapiMenu.mts

      - name: Build the doc
        working-directory: docs
        run: npm run build

