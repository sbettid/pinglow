name: Pinglow CI

on: [push, pull_request]

jobs:
  rust-ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run tests
        run: cargo test --all
